# 4.14.0 cherry-pick: commits pulados

Este documento registra os commits da `origin/develop` que nao foram integrados no branch local `4.14.0` durante o cherry-pick ate `73ba0b26e53d1ce94431f13f45c913ca77148edf`.

## Criterios usados

- Preservar a arquitetura local ViperChat para chamadas/voz.
- Evitar reintroduzir componentes upstream de Voice Calls que dependem de arquivos removidos ou substituidos localmente.
- Evitar sobrescrever branding, white-label e traducoes locais em massa.
- Manter commits de feature/fix independentes quando nao dependiam da pilha pulada.

## Commits pulados

| Commit | Titulo | Motivo |
| --- | --- | --- |
| `c4a6a19e9` | `feat(voice): WhatsApp Cloud Calling — UI [6] (#14346)` | Pulado por depender da pilha upstream de WhatsApp/Voice Calls e conflitar com a arquitetura local Viper de chamadas. |
| `68e358d73` | `feat: voice-call UX fixes (#14579)` | Pulado por ser ajuste de UX sobre a mesma pilha de Voice Calls que nao foi integrada. |
| `de8939103` | `fix: use hang-up handset icon for reject/end call button (#14640)` | Pulado porque altera `CallCard.vue`, componente da UI upstream de chamadas que esta removido/substituido localmente. |
| `0e87519e` | `feat: add mute button for Twilio calls (#14637)` | Pulado porque altera `FloatingCallWidget.vue` e cliente Twilio de chamadas, dependentes da UI upstream de calls. |
| `94ddd9805` | `chore: update the voice call ringtone (#14636)` | Pulado porque adiciona asset/comportamento para a UI upstream de chamadas que nao foi integrada. |
| `0a181b0ce` | `chore: Update translations (#14498)` | Pulado porque abriu conflitos massivos em arquivos de locale e tinha alto risco de sobrescrever branding, white-label e traducoes locais. |

## Commit integrado e revertido localmente

| Commit | Titulo | Acao | Motivo |
| --- | --- | --- | --- |
| `ea910227a` | `feat: add Voice Calls feature card to settings showcase (#14635)` | Integrado sem conflito e revertido em `612c480f4`. | O card expunha Voice Calls nas configuracoes, mas a pilha funcional upstream de chamadas foi pulada. |

## Commit de adaptacao relacionado

| Commit | Titulo | Motivo |
| --- | --- | --- |
| `612c480f4` | `Revert "feat: add Voice Calls feature card to settings showcase (#14635)"` | Remove o card de Voice Calls para nao anunciar uma feature cuja pilha upstream nao foi integrada. |
| `2fdf47bc0` | `fix(viperchat): clean up cherry-pick whitespace` | Limpa whitespace residual em `app/javascript/shared/constants/messages.js` apos resolucao de conflitos. |

## Observacoes

- O suporte a mensagem de voz do WhatsApp (`37eed5de1`) foi integrado, pois trata envio de audio/OGG como mensagem e nao depende da pilha de chamadas em tempo real.
- O branch `4.14.0` ficou em `4.14.1-viper`, preservando `@viperteccoporation/viperchat` e `UNLICENSED`.
